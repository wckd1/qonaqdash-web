/** List row, `{ id }` deep-link stub, or grid-style ref for `GuestSidePanel`. */
export interface GuestSidePanelRef {
  id: string
  first_name?: string
  last_name?: string
  email?: string
  phone?: string
}
